'use strict';

describe('frontendApp.exam module', function () {

    beforeEach(module('frontendApp.exam'));

    describe('exam controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var examCtrl = $controller('ExamCtrl');
            expect(examCtrl).toBeDefined();
        }));

    });
});